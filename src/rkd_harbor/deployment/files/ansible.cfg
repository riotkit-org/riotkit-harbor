[ssh_connection]
ssh_args = -o ForwardAgent=yes -o ControlMaster=auto -o ControlPersist=60s
pipelining = True

[defaults]
transport = ssh
sudo_flags = -H -E -S

[sudo_become_plugin]
flags = -H -E -S

